# .env.example - TEMPLATE PARA DESENVOLVIMENTO
# COPIE para .env.local e preencha os valores
# NÃO COMMITAR .env.local NO GIT!

# ============================================
# CONFIGURAÇÃO DO BANCO DE DADOS (Local)
# ============================================
DB_USER=postgres
DB_HOST=localhost
DB_NAME=SneakerLabsDB
DB_PASSWORD=senai
DB_PORT=5432

# ============================================
# OPENAI API (Opcional - para mensagens IA)
# ============================================
# Deixe VAZIO para usar modo local (fallback)
# Ou use chave de TESTE (nunca chave de produção)
OPENAI_API_KEY=

# ============================================
# CONFIGURAÇÃO DO SERVIDOR
# ============================================
SERVER_PORT=3001
NODE_ENV=development

# ============================================
# MIDDLEWARE AWS (Opcional)
# ============================================
MIDDLEWARE_URL=http://52.1.197.112:3000
USAR_MIDDLEWARE_REAL=false